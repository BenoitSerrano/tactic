type examApiType = {
    id: string;
    name: string;
    duration: number | null;
    startText: string;
    endText: string;
    createdAt: string;
    archivedAt: string | null;
};

export type { examApiType };
